"use client";

import { ScannerWorkspace } from "../../scan/page";

/** Customer route adapter: manager scanner UI, assigned-engagement transport. */
export default function PortalScannerPage() {
  return <ScannerWorkspace surface="portal" />;
}
